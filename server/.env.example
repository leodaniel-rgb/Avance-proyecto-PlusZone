# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=leo
DB_PASSWORD=Canalyt52#
DB_NAME=pluszone

# Email (SMTP) - ejemplo usando TurboSMTP (ajusta según tu proveedor)
EMAIL_HOST=pro.eu.turbo-smtp.com
EMAIL_PORT=465
EMAIL_SECURE=true
# SMTP auth user (consumer key)
EMAIL_USER=16ea7fc56ad7fbe7074b
# SMTP auth pass (consumer secret)
EMAIL_PASS=jAtpNlLaCsFKR1ObWqxH
# Dirección FROM visible a los usuarios
EMAIL_FROM=PlusZone1@outlook.com
# Si SMTP no está disponible, el servidor puede guardar códigos en `server/verification_debug.log` (true/false)
EMAIL_FALLBACK=true

# TurboSMTP API (opcional) - habilita para usar la API REST en lugar de SMTP
EMAIL_API_ENABLED=false
EMAIL_API_BASE=https://pro.api.serversmtp.com/api/v2
EMAIL_CONSUMER_KEY=
EMAIL_CONSUMER_SECRET=

# Puerto del servidor
PORT=4000

# Código de verificación: duración en minutos
VERIFICATION_EXPIRATION_MINUTES=15
